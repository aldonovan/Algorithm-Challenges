#include <bits/stdc++.h>
using namespace std;
int main() {
    //Given game c, want to find its children. Starts with subtract 4, that decreases by one each time, pretty simple
    //Maybe reverse the string, and then it should be easier. 2n + 1 and 2n + 2 as children, (n - 1) / 2 for parent 
    //Maybe represent it as a binary tree? Store values in nodes? Can traverse the tree to get answer
    return 0;
}
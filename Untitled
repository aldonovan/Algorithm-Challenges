#include <iostream>
#include <algorithm>
using namespace std;
int main() {
  //Need to detect if a subsegment is a ladder
  int n, m;
  cin >> n >> m;
  int a[n];
  for(int i = 0; i < n; i++) {
    cin >> a[i];
  }
  return 0;
}
